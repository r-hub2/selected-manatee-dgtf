% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{as_dgtf_sim}
\alias{as_dgtf_sim}
\title{Coerce a list of simulated data to a \code{dgtf_sim} object}
\usage{
as_dgtf_sim(x, model, ...)
}
\arguments{
\item{x}{A list with at least an element \code{y} (numeric vector of
observations). May also include \code{psi}, \code{lambda}, \code{ft}, \code{prob},
\code{z} (numeric vectors of the same length as \code{y}), \code{Theta}
(numeric matrix), and \code{nlag} (integer scalar).}

\item{model}{A \code{dgtf_model} whose \strong{fixed parameter values are
the ground-truth values used to generate \code{x}}. Its gain
function is also what's used to derive \code{R_t} from \code{psi} in
\code{plot(., what = "Rt")}.}

\item{...}{Unused.}
}
\value{
An object of class \code{dgtf_sim}.
}
\description{
Wraps externally-generated simulated data (e.g. from another
simulator, or a hand-built study) into a \code{dgtf_sim} so the
existing \code{print()} and \code{plot()} methods
methods, and any model-check workflows that key off \code{psi} /
\code{model} as the ground truth, work on it.
}
\section{Important}{

The rest of the package treats \code{psi} and the parameters in
\code{model} as the \strong{truth} for model-checking purposes (e.g.
\code{plot(fit, what = "Rt", truth = log1p(exp(sim$psi)))},
coverage / interval-score calculations in
\code{\link[=posterior_predict]{posterior_predict()}}). Pass only values that actually came
from a forward simulation under the same \code{model}. Arbitrary
numbers will produce meaningless model-checking comparisons.
}

\examples{
\dontrun{
# External simulation: any code that draws from a generative
# process described by `mod`. Here we just show the shape:
mod <- dgtf_hawkes()
raw <- list(y = my_y, psi = my_psi)   # at minimum, `y`
sim <- as_dgtf_sim(raw, model = mod)
plot(sim, what = "Rt")
}
}
