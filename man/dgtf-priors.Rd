% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{dgtf-priors}
\alias{dgtf-priors}
\alias{normal}
\alias{inv_gamma}
\alias{dist_gamma}
\alias{dist_beta}
\alias{uniform}
\alias{half_normal}
\alias{half_cauchy}
\alias{half_t}
\title{Prior distribution constructors}
\usage{
normal(mean = 0, sd = 1)

inv_gamma(shape = 1, scale = 1)

dist_gamma(shape = 1, rate = 1)

dist_beta(alpha = 1, beta = 1)

uniform(lower = 0, upper = 1)

half_normal(sd = 1)

half_cauchy(scale = 1)

half_t(df = 3, scale = 1)
}
\arguments{
\item{mean, sd, shape, scale, rate, alpha, beta, lower, upper, df}{Hyperparameters.}
}
\value{
An object of class \code{dgtf_prior_dist}.
}
\description{
Lightweight constructors for the prior distributions accepted by \code{dgtf()}.
Each returns an object of class \code{dgtf_prior_dist} carrying the
distribution name (as the C++ engine recognises it) and a numeric vector
of hyperparameters.
}
\details{
Supported distributions and their (par1, par2) interpretations:
\itemize{
\item \code{normal(mean, sd)}
\item \code{inv_gamma(shape, scale)}
\item \code{dist_gamma(shape, rate)} (renamed to avoid masking \code{base::gamma})
\item \code{dist_beta(alpha, beta)} (renamed to avoid masking \code{base::beta})
\item \code{uniform(lower, upper)}
\item \code{half_normal(sd)}         (par2 unused; kept for ABI parity)
\item \code{half_cauchy(scale)}
\item \code{half_t(df, scale)}
}
}
\examples{
normal(0, 1)
inv_gamma(2, 1)
}
