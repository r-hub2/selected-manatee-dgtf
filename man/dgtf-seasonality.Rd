% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seasonality.R
\name{dgtf-seasonality}
\alias{dgtf-seasonality}
\alias{seas_none}
\alias{seas_weekly}
\alias{seas_period}
\title{Seasonality constructors}
\usage{
seas_none()

seas_weekly(init = rep(0, 7L), in_state = FALSE)

seas_period(period, init = rep(0, period), in_state = FALSE)
}
\arguments{
\item{init}{Seasonal-effect values. Scalar (recycled), or a vector of
length \eqn{\geq} \code{period}.}

\item{in_state}{If \code{TRUE}, treat the seasonal effects as part of the
latent state evolved by the system equation.}

\item{period}{Integer seasonal period.}
}
\value{
A \code{dgtf_seasonality} object.
}
\description{
Specify the period, seasonal effects, and whether seasonality enters
via the latent state vector or as a fixed deterministic offset.
}
\details{
\itemize{
\item \code{seas_none()}: no seasonality (period 1, init 0). The model has
no intercept either; supply one via \code{dgtf_model(intercept = ...)}.
\item \code{seas_weekly(init, in_state)}: weekly seasonality (period 7).
\item \code{seas_period(period, init, in_state)}: arbitrary period.
}

\code{init} is the seasonal-effect vector. A scalar is recycled to length
\code{period}. A vector of length \eqn{\geq} \code{period} is accepted as-is;
only the first \code{period} elements form the seasonal pattern, but the
extras may be used by the C++ engine to seed the latent state when
\code{in_state = TRUE}.

Note that when seasonality has period \eqn{> 1} the seasonal effects
already encode the level of the series; there is no separate
"intercept". \code{dgtf_model()} will warn if both \code{intercept} and a
non-trivial \code{seasonality} are set.
}
\examples{
seas_none()
seas_weekly(init = c(0.9, 1.0, 1.1, 1.0, 0.9, 0.8, 1.3))
seas_period(period = 2, init = c(1, 2))
}
