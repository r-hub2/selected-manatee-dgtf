% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgtf.R
\name{dgtf}
\alias{dgtf}
\title{Fit a DGTF model}
\usage{
dgtf(
  y,
  model,
  prior = dgtf_prior(),
  method = c("hva", "mcmc"),
  control = NULL,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{y}{Numeric vector of observed counts (or, in future versions,
a matrix for multivariate observations).}

\item{model}{A \code{\link{dgtf_model}} object.}

\item{prior}{A \code{\link{dgtf_prior}} object (default: empty -> all parameters fixed).}

\item{method}{\code{"hva"} or \code{"mcmc"}.}

\item{control}{A method-specific control object. If \code{NULL}, sensible
defaults are used.}

\item{seed}{Optional integer seed.}

\item{verbose}{If \code{TRUE}, returns the lowered model and method
settings \emph{without} calling the C++ engine. Useful for debugging
what gets handed to the engines.}
}
\value{
An object of class \code{dgtf_fit}.
}
\description{
The user-facing entry point for inference on a DGTF model. Wraps the
low-level C++ inference engines and returns an S3 \code{dgtf_fit}
object with familiar methods (\code{print}, \code{summary}, \code{coef}, \code{predict},
...).
}
\section{Choosing a method}{

\itemize{
\item \code{"hva"}: hybrid variational approximation that combines SMC and
variational updates (Tang et al., the engine featured in the
manuscript). Fast; good for real-time applications. Also tuned via
\code{\link[=vb_control]{vb_control()}}.
\item \code{"mcmc"}: disturbance Metropolis-Hastings within Gibbs.
Gold-standard uncertainty quantification; slower. Use
\code{\link[=mcmc_control]{mcmc_control()}} to tune.
}
}

\section{Inference rule}{

Anything supplied a prior in \code{prior} is treated as unknown and
inferred. Anything left out of \code{prior} is treated as fixed at the
value supplied to \code{\link[=dgtf_model]{dgtf_model()}}.
}

\examples{
\dontrun{
mod <- dgtf_hawkes()
set.seed(1)
sim <- dgtf_simulate(mod, ntime = 200)
fit <- dgtf(sim$y, mod,
            prior = dgtf_prior(W = inv_gamma(1, 1)),
            method = "hva",
            control = vb_control(iter = 200))
}
}
