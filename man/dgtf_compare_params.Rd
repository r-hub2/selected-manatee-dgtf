% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgtf-compare.R
\name{dgtf_compare_params}
\alias{dgtf_compare_params}
\title{Per-parameter posterior summaries across multiple fits}
\usage{
dgtf_compare_params(fits, truth = NULL, level = 0.95)
}
\arguments{
\item{fits}{Named list of \code{dgtf_fit} objects.}

\item{truth}{Optional named numeric vector or named list of true
static parameter values (same convention as
\code{\link[=param_recovery]{param_recovery()}}). When \code{NULL} (default), only posterior
summaries are reported.}

\item{level}{Credible-interval level for the \code{q_lo}, \code{q_hi}, and
(when \code{truth} is supplied) \code{coverage} columns (default 0.95).}
}
\value{
A data frame of class \code{dgtf_param_comparison} with columns
\code{parameter}, \code{model}, \code{mean}, \code{median}, \code{sd}, \code{q_lo}, \code{q_hi},
and, when \code{truth} is supplied, \code{truth}, \code{bias}, \code{mae}, \code{rmse},
\code{crps}, \code{coverage}. Rows are ordered by parameter, then by the
input order of \code{fits}.
}
\description{
Long-form table stacking per-parameter posterior summaries for every
fit in \code{fits}, with an added \code{model} column. One row per
(parameter, fit) pair. Use this for the detailed cross-fit view of
static-parameter estimation.
}
\details{
When \code{truth} is supplied, the table additionally reports recovery
metrics (\code{bias}, \code{mae}, \code{rmse}, \code{crps}, \code{coverage}) from
\code{\link[=param_recovery]{param_recovery()}}. When \code{truth} is \code{NULL}, only the posterior
summaries (\code{mean}, \code{median}, \code{sd}, \code{q_lo}, \code{q_hi}) are reported,
giving a side-by-side view of the marginal posteriors without
assuming a known data-generating process.

For an at-a-glance summary across fits, use \code{\link[=dgtf_compare]{dgtf_compare()}}
(with \code{truth} supplied for the recovery columns).
}
