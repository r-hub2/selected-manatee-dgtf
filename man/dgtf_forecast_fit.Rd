% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-predict.R
\name{dgtf_forecast_fit}
\alias{dgtf_forecast_fit}
\title{Out-of-sample forecasts from a DGTF fit}
\usage{
dgtf_forecast_fit(
  object,
  h = 1L,
  nrep = 100L,
  ypred_true = NULL,
  only_last = FALSE,
  return_samples = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{dgtf_fit}.}

\item{h}{Forecast horizon (positive integer).}

\item{nrep}{Number of replicate draws per posterior sample. The
total number of forecast draws is \verb{nrep * <number of posterior samples>}.}

\item{ypred_true}{Optional numeric vector of held-out future
observations (length \verb{>= h}). When supplied, scoring metrics
(\code{coverage}, \code{chi}, \code{crps}) are computed against the last \code{h}
elements (or the last single value when \code{only_last = TRUE}).
includes per-step coverage and error metrics.}

\item{only_last}{If \code{TRUE}, only the \code{h}-step-ahead prediction is
reported (intermediate horizons are skipped).}

\item{return_samples}{If \code{TRUE}, retains the full posterior
predictive forecast draws in \code{samples}.}

\item{...}{Unused.}
}
\value{
An object of class \code{dgtf_forecast} (a list):
\itemize{
\item \code{quantiles} — numeric matrix with columns \code{lower}, \code{median},
\code{upper} at the 95\% level. Has \code{h} rows by default, or \code{1} row
when \code{only_last = TRUE}.
\item \code{level} — credible-interval level (currently fixed at \code{0.95}).
\item \code{horizon} — the requested forecast horizon \code{h}.
\item \code{only_last}, \code{nsample} — flags / total forecast-draw count.
\item \code{samples} — matrix of posterior predictive draws (only when
\code{return_samples = TRUE}).
\item \code{ypred_true} — supplied truth vector (when given).
\item \code{coverage}, \code{chi}, \code{crps} — scoring metrics (when \code{ypred_true}
is supplied).
}
}
\description{
\eqn{h}-step-ahead posterior predictive forecasts, optionally
scored against held-out future observations.
}
\examples{
\dontrun{
fit <- dgtf(sim$y[1:180], mod, prior, method = "hva")
fc  <- dgtf_forecast_fit(fit, h = 20, nrep = 200,
                         ypred_true = sim$y[181:200])
fc
summary(fc)
}
}
