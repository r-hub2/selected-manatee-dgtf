% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{dgtf_model}
\alias{dgtf_model}
\title{Build a DGTF model}
\usage{
dgtf_model(
  obs,
  link,
  sys,
  gain = gain_identity(),
  lag = lag_uniform(window = 1L),
  err = err_gaussian(W = 0.01),
  intercept = 0,
  seasonality = NULL,
  obs_init = c(0, 30),
  zi = FALSE
)
}
\arguments{
\item{obs}{Observation distribution. See \code{\link{dgtf-obs}}.}

\item{link}{Link function. See \code{\link{dgtf-link}}.}

\item{sys}{System equation. See \code{\link{dgtf-sys}}.}

\item{gain}{Gain function. See \code{\link{dgtf-gain}}.}

\item{lag}{Lag distribution. See \code{\link{dgtf-lag}}.}

\item{err}{System-error distribution. See \code{\link{dgtf-err}}.}

\item{intercept}{Numeric scalar baseline. Used \strong{only} when
\code{seasonality} is \code{NULL} / \code{seas_none()}; otherwise the seasonal
effects already carry the level.}

\item{seasonality}{A \code{\link[=dgtf-seasonality]{dgtf_seasonality}} object,
\code{NULL} for none, or a single integer period (uses zero seasonal
effects).}

\item{obs_init}{Numeric vector of initial observation-distribution
parameters (default \code{c(0, 30)} matches the historical raw-list API).}

\item{zi}{Logical; whether the observation model is zero-inflated.}
}
\value{
An object of class \code{dgtf_model}.
}
\description{
Assembles the components of a dynamic generalised time-series filter
(DGTF) model. The returned object captures all structural choices and
fixed-parameter values, and is what \code{dgtf()} and \code{dgtf_simulate_model()}
operate on.
}
\details{
Components may be supplied either as full constructor objects
(\code{obs_nbinom()}, \code{lag_lognormal(...)}, ...) or as the corresponding
string shortcuts (\code{"nbinom"}, \code{"lognorm"}, ...). The longhand form is
what makes the framework extensible: a custom component can be written
by following the protocol in \code{?dgtf-extending} (forthcoming).

Anything supplied as a numeric value (e.g. \code{intercept = 0},
\code{err = err_gaussian(W = 0.01)}) is treated as \strong{fixed}. To infer it,
supply a prior in \code{\link[=dgtf_prior]{dgtf_prior()}}.
}
\section{Intercept and seasonality}{


The seasonality block already encodes the level of the series, so
\code{intercept} and a non-trivial \code{seasonality} are mutually exclusive:
\itemize{
\item \code{dgtf_model(intercept = a)} (with \code{seasonality} left \code{NULL}) is
equivalent to \code{dgtf_model(seasonality = seas_period(1, init = a))}
— the intercept is the single seasonal effect under period 1.
\item \code{dgtf_model(seasonality = seas_weekly(init = c(...)))} includes the
level inside the seasonal pattern; do not also set \code{intercept}.
Setting both produces a warning and the intercept is ignored.
}
}

\examples{
# Period-1 model: intercept = 5
dgtf_model(obs_poisson(), link_identity(), sys_identity(), intercept = 5)

# Period-2 deterministic seasonality with explicit pattern
dgtf_model(obs_poisson(), link_identity(), sys_identity(),
           seasonality = seas_period(period = 2, init = c(1, 2)))

# Discretised Hawkes with weekly seasonality
dgtf_model(
  obs_nbinom(), link_identity(), sys_shift(),
  gain        = gain_softplus(),
  lag         = lag_lognormal(),
  seasonality = seas_weekly(init = c(0.9, 1.0, 1.1, 1.0, 0.9, 0.8, 1.3))
)
}
