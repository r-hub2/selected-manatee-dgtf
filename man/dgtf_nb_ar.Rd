% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical-models.R
\name{dgtf_nb_ar}
\alias{dgtf_nb_ar}
\title{Negative binomial AR model with time-varying coefficients}
\usage{
dgtf_nb_ar(p = 3L, gain = "softplus", W = 0.01, seasonality = seas_none())
}
\arguments{
\item{p}{Integer. Autoregressive order. Default 3.}

\item{gain}{Character or \code{dgtf_gain} object. Default
\code{"softplus"}.}

\item{W}{Numeric. Scalar innovation variance (applied as
\eqn{W \mathbf{I}_p}). Default 0.01.}

\item{seasonality}{A \code{dgtf_seasonality} object. Default
\code{seas_none()}.}
}
\value{
A \code{dgtf_model} object.
}
\description{
Constructs a negative binomial AR(p) model. The intensity is
\deqn{\lambda_t = \varphi + \sum_{i=1}^{p} h(\psi_{i,t}) y_{t-i}}
where each \eqn{\psi_{i,t}} follows an independent random walk.
}
\details{
The C++ engine uses \code{sys_eq = "identity"} (\eqn{G = I_p}) and
\code{lag_dist = "uniform"} (\eqn{\phi_l \equiv 1}). With
\code{full_rank = TRUE}, all p states receive independent noise.
}
\examples{
m <- dgtf_nb_ar(p = 3, seasonality = seas_weekly())
}
