% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{dgtf_prior}
\alias{dgtf_prior}
\title{Aggregate priors for inference}
\usage{
dgtf_prior(
  intercept = NULL,
  seasonality = NULL,
  W = NULL,
  rho = NULL,
  lag = NULL,
  ...
)
}
\arguments{
\item{intercept}{Prior on the baseline \code{a} (regression intercept).}

\item{seasonality}{Prior on the seasonality effect.}

\item{W}{Prior on the system-error variance.}

\item{rho}{Prior on the negative-binomial dispersion.}

\item{lag}{A named list of priors on the lag-distribution parameters,
e.g. \code{list(par1 = normal(0, 1), par2 = inv_gamma(1, 1))} (or use
distribution-specific names like \code{meanlog}/\code{sdlog}).}

\item{...}{Additional named priors on other model parameters (reserved
for future extensions; currently passed through unchanged).}
}
\value{
An object of class \code{dgtf_prior}.
}
\description{
Collects per-parameter prior specifications. Anything passed here is
treated as an unknown to be inferred; anything left out is treated as
fixed at the value supplied to \code{dgtf_model()}.
}
\examples{
dgtf_prior(
  seasonality = normal(1, 1),
  W           = inv_gamma(1, 1),
  rho         = inv_gamma(1, 1),
  lag         = list(par1 = normal(0, 1), par2 = inv_gamma(1, 1))
)
}
