% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{dgtf_simulate_model}
\alias{dgtf_simulate_model}
\title{Simulate from a DGTF model}
\usage{
dgtf_simulate_model(model, ntime, seed = NULL, y0 = 0, z = NULL)
}
\arguments{
\item{model}{A \code{dgtf_model} object.}

\item{ntime}{Length of the simulated series.}

\item{seed}{Optional RNG seed.}

\item{y0}{Initial observation (default 0).}

\item{z}{Optional binary mask of length \code{ntime + 1} enforcing
structural zeros at the indicated time points. Meaningful only
for zero-inflated models. Positions with \code{z = 0} are forced to
\code{y = 0}, while positions with \code{z = 1} are simulated from the
observation distribution as usual. Default \code{NULL} (no mask).}
}
\value{
An object of class \code{dgtf_sim} (a list) with components
\code{y}, \code{lambda}, \code{psi}, \code{Theta}, \code{ft}, \code{nlag}, plus the originating
\code{model} and the gain-function name \code{gain} used to map \code{psi} to
\code{R_t}. When the model is zero-inflated, \code{prob} and \code{z} are also
present.
}
\description{
Draws an observation series and the corresponding latent trajectories
from a \code{\link[=dgtf_model]{dgtf_model()}}.
}
\examples{
mod <- dgtf_hawkes()
sim <- dgtf_simulate_model(mod, ntime = 200, seed = 1)
print(sim)
plot(sim)
plot(sim, what = "Rt")
}
