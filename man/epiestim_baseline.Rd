% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epiestim-baseline.R
\name{epiestim_baseline}
\alias{epiestim_baseline}
\title{EpiEstim R_t baseline for model comparison}
\usage{
epiestim_baseline(
  y,
  mean_si,
  sd_si,
  method = c("parametric_si", "uncertain_si", "wallinga_teunis"),
  window = 7L,
  level = 0.95,
  align = c("center", "right"),
  mean_sd_si = 1.8,
  sd_sd_si = 1.4,
  ...
)
}
\arguments{
\item{y}{Numeric vector of incident counts (the same vector you
pass to \code{dgtf()}). For daily data with weekly seasonality you
typically want to deseasonalise \code{y} upstream before calling
EpiEstim, since the renewal model assumes a smooth incidence
signal.}

\item{mean_si, sd_si}{Serial-interval \strong{mean} and \strong{standard
deviation} in the same time units as \code{y}. Note these are the
mean / sd, not the variance.}

\item{method}{One of:
\itemize{
\item \code{"parametric_si"} (default) -- known SI mean / sd;
\item \code{"uncertain_si"} -- propagates uncertainty in SI mean / sd
using \code{mean_sd_si}, \code{sd_sd_si} as prior standard deviations;
\item \code{"wallinga_teunis"} -- case-reproduction-number method.
}}

\item{window}{Sliding-window width in time units (default 7).
Smaller windows give noisier, more responsive estimates.}

\item{level}{Credible-interval level (default 0.95). EpiEstim
natively reports 0.025 / 0.975 quantiles; for any other level
the bounds are derived from a Normal approximation to the
posterior (mean ± z * Std(R)) and a warning is emitted.}

\item{align}{Alignment of the EpiEstim window summary in the returned
matrix: \code{"center"} places each estimate near the middle of its window,
while \code{"right"} places it at the window end.}

\item{mean_sd_si, sd_sd_si}{Prior standard deviations on the SI
mean and SI sd respectively. Used only by
\code{method = "uncertain_si"}.}

\item{...}{Additional arguments forwarded to
\code{EpiEstim::make_config()} (e.g. \code{n1}, \code{n2}, \code{n_sim}).}
}
\value{
An object of class \code{"epiestim_baseline"} -- a
\verb{length(y) x 3} numeric matrix with columns \code{lower}, \code{median},
\code{upper}. The first \code{window} rows are \code{NA} (no estimate yet).
Method / level / SI parameters are stored as attributes.
}
\description{
Thin wrapper around \code{EpiEstim::estimate_R()} and
\code{EpiEstim::wallinga_teunis()} that returns posterior R_t bands
in a format consumable by \code{dgtf_compare_plot()} -- a
\verb{length(y) x 3} numeric matrix with columns \code{lower}, \code{median},
\code{upper}, padded with leading NAs so row \code{i} corresponds to time
\code{i} in the input series (the same convention as
\code{dgtf_fit$fit$psi}).
}
\details{
Intended use: drop a renewal-equation baseline next to \code{dgtf_fit}
objects in the comparison plot.

\if{html}{\out{<div class="sourceCode">}}\preformatted{fit_hva  <- dgtf(y, mod, prior, method = "hva")
fit_mcmc <- dgtf(y, mod, prior, method = "mcmc")
epi      <- epiestim_baseline(y, mean_si = 4.7, sd_si = 2.9)

dgtf_compare_plot(
    list(HVA = fit_hva, MCMC = fit_mcmc, EpiEstim = epi),
    what = "Rt")
}\if{html}{\out{</div>}}
}
