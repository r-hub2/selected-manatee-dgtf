% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param-recovery.R
\name{param_recovery}
\alias{param_recovery}
\title{Posterior recovery of static parameters against truth}
\usage{
param_recovery(object, truth, level = 0.95, digits = 3)
}
\arguments{
\item{object}{A \code{dgtf_fit}.}

\item{truth}{Named numeric vector OR named list of true static
parameter values. Names follow the same convention as the
columns of \code{as_draws_matrix(fit)}: scalar params use the bare
name (\code{"W"}, \code{"rho"}, \code{"lag_par1"}, \code{"lag_par2"},
\code{"zintercept"}, \code{"zzcoef"}); multi-element params use bracketed
indices (\code{"seas[1]"}, \code{"seas[2]"}, ...). When passing a list,
vector-valued entries are auto-expanded
(\code{list(seas = c(0.3, 0.5))} becomes \verb{seas[1] = 0.3, seas[2] = 0.5}).
Unknown names are ignored with a warning.}

\item{level}{Credible-interval level for the \code{coverage} column
(default \code{0.95}).}

\item{digits}{Number of digits to round the numeric columns to
for display (default \code{3}).}
}
\value{
A data.frame of class \code{param_recovery} with one row per
matched parameter and columns \code{parameter}, \code{truth}, \code{mean},
\code{median}, \code{sd}, \code{q_lo}, \code{q_hi}, \code{bias} (\code{mean - truth}), \code{mae},
\code{rmse}, \code{crps}, \code{coverage} (logical: truth in \verb{[q_lo, q_hi]}).
The default print method suppresses the \code{coverage} column and
instead appends a \code{Signif} column whose value is \code{"*"} when the
truth lies inside the credible interval, with the convention
explained in a footer (cf. \code{print.summary.lm}).
Returns \code{NULL} if there are no posterior draws or no name
matches.
}
\description{
Compares per-parameter posterior summaries (mean, sd, credible
interval) to user-supplied true values. Useful for simulation
studies and SBC-style aggregation across replicates.
}
\examples{
\dontrun{
fit <- dgtf(sim$y, mod, prior, method = "hva")
param_recovery(fit,
               truth = list(W    = sim$model$err$par1,
                            rho  = sim$model$rho,
                            seas = sim$model$seasonality$val))
}
}
