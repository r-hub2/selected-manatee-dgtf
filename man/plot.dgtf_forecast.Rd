% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.dgtf_forecast}
\alias{plot.dgtf_forecast}
\title{Plot a posterior-predictive forecast}
\usage{
\method{plot}{dgtf_forecast}(
  x,
  truth = NULL,
  time = NULL,
  color = NULL,
  alpha = 0.5,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  theme = ggplot2::theme_minimal(),
  ...
)
}
\arguments{
\item{x}{A \code{dgtf_forecast}.}

\item{truth}{Optional numeric vector of true future values to
overlay (same length convention as \code{ypred_true} in
\code{\link[=dgtf_forecast_fit]{dgtf_forecast_fit()}}: at least \code{horizon} entries for the full
trajectory, at least 1 for \code{only_last}). Defaults to
\code{x$ypred_true} if it was baked into the forecast object.}

\item{time}{Optional numeric vector of length \code{horizon} giving the
x-axis values (e.g. real dates coerced to numeric). Defaults to
\code{seq_len(horizon)}.}

\item{color}{Optional color for the forecast band (line + ribbon).
Defaults to black for a single-method forecast plot.}

\item{alpha}{Ribbon transparency (default 0.5).}

\item{xlab, ylab, main}{Axis / title labels. NULL falls back to
sensible defaults (\code{"Forecast step"} and \code{expression(hat(y))}).}

\item{theme}{A ggplot2 theme.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
Time-series plot of the forecast quantile band (line + ribbon),
with an optional truth overlay drawn as a dashed black line. This
matches the convention of the other dgtf time-series plotting
methods.
}
\details{
Single-step forecasts (\code{only_last = TRUE} in the original
\code{\link[=dgtf_forecast_fit]{dgtf_forecast_fit()}} call) are rendered as a point-and-interval
instead of a line-and-ribbon, since there's only one (h, y) pair
to show; the truth, if present, is drawn as a black "x" at the
same time step.
}
