% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-lag.R
\name{plot_dgtf_lag}
\alias{plot_dgtf_lag}
\title{Lag-distribution comparison plot}
\usage{
plot_dgtf_lag(
  fits,
  window = NULL,
  xlim = NULL,
  normalize = c("pmf", "raw", "branching"),
  summary = c("median", "mean"),
  alpha = 0.5,
  palette = NULL,
  xlab = "Lag",
  ylab = NULL,
  main = NULL,
  theme = ggplot2::theme_minimal(),
  ...
)
}
\arguments{
\item{fits}{Either a \code{dgtf_fit} (treated as a singleton list with
name \code{"Estimate"}), or a named list of \code{dgtf_fit} objects.}

\item{window}{Maximum lag to display. \code{NULL} (default) uses the
largest lag window across all fits.}

\item{xlim}{Display-only truncation: maximum lag to show in the
plot. Defaults to \code{window} (no truncation). Useful when the
underlying lag window is wide (e.g. 30 days) but you only want
to show the first 10 days. Weights are still computed and
normalized over the full \code{window}, so the visible bars retain
their correct relative mass; the tail past \code{xlim} is implicit.}

\item{normalize}{One of:
\itemize{
\item \code{"pmf"} (default): rescale each model's weights to sum to 1.
Compares lag \emph{shapes}; hides the AR / branching-ratio scale.
\item \code{"raw"}: show as-is. Hawkes and DL/Koyck stay near unit mass
(truncation residual aside); AR shows the raw branching
weights \code{h(theta_l)}. Most honest when magnitude matters.
\item \code{"branching"}: rescale each PMF to integrate to the model's
mean \code{R_t} (estimated as the time-mean of \code{h(psi)}); for AR
the natural sum is \code{sum(h(theta_l))}. Same shape information
as \code{"pmf"} with absolute magnitudes restored.
}}

\item{summary}{Posterior summary applied to each model's
parameter / coefficient draws before computing weights.
\code{"median"} (default) or \code{"mean"}.}

\item{alpha}{Bar transparency (default 0.5).}

\item{palette}{Optional named character vector of fill colours.
Same convention as \code{dgtf_compare_plot()}.}

\item{xlab, ylab, main}{Axis / title labels. \code{ylab = NULL} (default)
picks a label that matches \code{normalize}.}

\item{theme}{A ggplot2 theme.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
Compares per-lag weights across one or more \code{dgtf_fit} objects on a
single grouped bar chart. Useful for characterizing how different
DGTF model variants (Hawkes / distributed lag / Koyck / AR)
distribute past-observation influence across lags.
}
\details{
Three lag-weight conventions are unified onto a common visual:
\itemize{
\item \strong{Hawkes} (\code{sys = shift}, \code{lag = lognormal}): the discretized
lognormal PMF at posterior-median (\code{meanlog}, \code{sigma2}), via
\code{dlognorm0()}.
\item \strong{Distributed lag / Koyck} (\code{sys = nbinom} or \code{shift},
\code{lag = nbinom}): the discretized NB PMF at posterior-median
(\code{kappa}, \code{r}), via \code{dnbinom0()}. Both system forms share the
same NB lag weights; only the underlying state-space
representation differs.
\item \strong{AR} (\code{sys = identity}, \code{lag = uniform}): the gain-applied
AR coefficient \code{h(theta_l)} summarized across time and posterior
samples for each lag \code{l}. These are \emph{not} probabilities — see
\code{normalize} for how they're placed on a comparable scale.
}
}
\examples{
\dontrun{
fit_hawkes <- dgtf(y, dgtf_hawkes(), prior, method = "hva")
fit_dl <- dgtf(y, dgtf_distributed_lag(r = 3), prior, method = "hva")
fit_ar <- dgtf(y, dgtf_nb_ar(p = 5), prior, method = "hva")

plot_dgtf_lag(list(
    Hawkes = fit_hawkes,
    DL3 = fit_dl,
    "AR(5)" = fit_ar
))

# Honest magnitudes (AR's raw branching weights look bigger than
# the unit-mass shift-system PMFs):
plot_dgtf_lag(list(Hawkes = fit_hawkes, "AR(5)" = fit_ar),
    normalize = "raw"
)
}
}
