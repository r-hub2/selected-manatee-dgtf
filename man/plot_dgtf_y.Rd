% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_dgtf_y}
\alias{plot_dgtf_y}
\title{In-sample fit + out-of-sample forecast in one plot}
\usage{
plot_dgtf_y(
  x,
  forecast = NULL,
  truth_in = NULL,
  truth_out = NULL,
  show_truth = TRUE,
  time_in = NULL,
  time_out = NULL,
  level = 0.95,
  nrep = 100L,
  in_color = "#0072B2",
  out_color = "#D55E00",
  in_name = "In-sample",
  out_name = "Forecast",
  alpha = 0.4,
  xlab = "Time",
  ylab = expression(y[t]),
  main = NULL,
  theme = ggplot2::theme_minimal(),
  ...
)
}
\arguments{
\item{x}{Either a \code{dgtf_fit} or a \code{dgtf_ppc} (the in-sample band
provider).}

\item{forecast}{Optional \code{dgtf_forecast} (from
\code{\link[=dgtf_forecast_fit]{dgtf_forecast_fit()}}). When \code{NULL}, only the in-sample band
is plotted.}

\item{truth_in}{Optional numeric vector of length \code{length(fit$y)}
to overlay as the in-sample truth. \code{NULL} (default) auto-fills
with \code{fit$y}.}

\item{truth_out}{Optional numeric vector of length
\code{forecast$horizon} to overlay as the out-of-sample truth. \code{NULL}
(default) auto-fills with \code{forecast$ypred_true} when present.}

\item{show_truth}{If \code{FALSE}, suppress all truth overlays
regardless of \code{truth_in} / \code{truth_out}. Default \code{TRUE}.}

\item{time_in, time_out}{Optional numeric vectors giving the x-axis
values for the two segments (e.g. real dates coerced to
numeric). Defaults: \code{0:(n-1)} for in-sample and
\code{n:(n+h-1)} for out-of-sample (with \code{n = length(fit$y)},
\code{h = forecast$horizon}), so the two segments connect.}

\item{level}{Credible-interval level used when the in-sample PPC
has to be computed on the fly (i.e. \code{attr(fit, "ppc")} is not
already attached).}

\item{nrep}{Posterior-predictive replicate count for the
on-the-fly PPC.}

\item{in_color, out_color}{Hex colours for the two bands. Default
to Okabe-Ito blue / vermillion for visual distinction.}

\item{in_name, out_name}{Legend labels for the two bands.}

\item{alpha}{Ribbon transparency (default 0.4).}

\item{xlab, ylab, main}{Axis / title labels.}

\item{theme}{A ggplot2 theme.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the posterior-predictive band for \code{y} over the in-sample
window and, optionally, the forecast band for \code{y} over the
out-of-sample window (from a \code{dgtf_forecast}), distinguished by
colour. Truth values for either segment are overlaid as a single
dashed black line spanning both windows.
}
\details{
The in-sample band can come from either:
\itemize{
\item a \code{dgtf_fit} -- its \code{attr(., "ppc")} is reused if present,
otherwise \code{\link[=posterior_predict]{posterior_predict()}} is run on the fly. The
observed \code{y} is used as the in-sample truth by default.
\item a \code{dgtf_ppc} -- used directly. No automatic in-sample truth
(the PPC object doesn't carry \code{y}); pass \code{truth_in} if you
want one.
}
}
\examples{
\dontrun{
fit <- dgtf(sim$y[1:180], mod, prior, method = "hva")
fc <- dgtf_forecast_fit(fit,
    h = 20, nrep = 200,
    ypred_true = sim$y[181:200]
)

# In-sample band + forecast band + dashed truth across the gap.
plot_dgtf_y(fit, fc)

# In-sample only (forecast = NULL); equivalent to a yhat PPC plot
# with the observed series overlaid.
plot_dgtf_y(fit)

# Real dates on the x-axis.
plot_dgtf_y(fit, fc,
    time_in  = as.numeric(date_seq[1:180]),
    time_out = as.numeric(date_seq[181:200]),
    xlab     = "Date"
)
}

}
