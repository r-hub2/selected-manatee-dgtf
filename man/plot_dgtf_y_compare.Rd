% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_dgtf_y_compare}
\alias{plot_dgtf_y_compare}
\title{Multi-method in-sample + forecast comparison plot}
\usage{
plot_dgtf_y_compare(
  fits,
  forecasts = NULL,
  truth_in = NULL,
  truth_out = NULL,
  show_truth = TRUE,
  time_in = NULL,
  time_out = NULL,
  level = 0.95,
  nrep = 100L,
  palette = NULL,
  alpha = 0.4,
  split_line = TRUE,
  xlab = "Time",
  ylab = expression(y[t]),
  main = NULL,
  theme = ggplot2::theme_minimal(),
  ...
)
}
\arguments{
\item{fits}{Named list. Each entry is a \code{dgtf_fit} or a \code{dgtf_ppc}.
\code{dgtf_fit} entries use their cached PPC if present
(\code{attr(., "ppc")}), otherwise run \code{\link[=posterior_predict]{posterior_predict()}} on the
fly with \code{nrep} and \code{level}.}

\item{forecasts}{Named list of \code{dgtf_forecast} objects, with names
matching a subset of \code{names(fits)}. Methods without a forecast
entry are drawn in-sample only. \code{NULL} (default) draws in-sample
only for every method.}

\item{truth_in}{Optional numeric vector for the in-sample truth.
\code{NULL} (default) auto-fills with the first fit's \verb{$y} (assumed
shared across methods).}

\item{truth_out}{Optional numeric vector for the out-of-sample
truth. \code{NULL} (default) auto-fills with the first forecast's
\verb{$ypred_true} when present.}

\item{show_truth}{\code{FALSE} suppresses all truth overlays.}

\item{time_in, time_out}{Optional numeric vectors giving the x-axis
values for the two segments. Defaults: \code{0:(n-1)} for in-sample
and \code{n:(n+h-1)} for out-of-sample, so the segments connect.}

\item{level, nrep}{Forwarded to \code{\link[=posterior_predict]{posterior_predict()}} for fits
without a cached PPC.}

\item{palette}{Optional named character vector of colours.}

\item{alpha}{Ribbon transparency (default 0.4).}

\item{split_line}{If \code{TRUE} (default), draw a dotted vertical line
at the train/test boundary. Set \code{FALSE} to suppress.}

\item{xlab, ylab, main}{Axis / title labels.}

\item{theme}{A ggplot2 theme.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object.
}
\description{
Side-by-side equivalent of \code{\link[=plot_dgtf_y]{plot_dgtf_y()}} for an arbitrary number
of method/model variants. For each method, an in-sample posterior
predictive band is drawn (from the fit's PPC, cached or computed
on the fly), and -- when a forecast is supplied -- a forecast band
is drawn continuing the time axis past the train/test boundary.
}
\details{
Each method gets a single colour applied to \emph{both} its in-sample
band and its forecast band; segments are visually distinguished by
a vertical dotted line at the train/test split rather than by a
second colour channel. This keeps the legend readable across many
methods.
}
