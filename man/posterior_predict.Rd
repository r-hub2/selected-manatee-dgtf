% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-predict.R
\name{posterior_predict}
\alias{posterior_predict}
\alias{posterior_predict.dgtf_fit}
\title{Posterior predictive draws}
\usage{
posterior_predict(object, ...)

\method{posterior_predict}{dgtf_fit}(object, nrep = 100L, Rt = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{dgtf_fit}.}

\item{...}{Unused.}

\item{nrep}{Number of replicate draws \strong{per posterior sample}. The
total number of posterior-predictive draws is
\verb{nrep * <number of posterior samples>} (number of samples is the
second dimension of \code{object$fit$psi_stored} / \code{object$fit$Theta}).}

\item{Rt}{Optional length-\code{length(y)} vector of "true" intensity
values used as a reference (e.g. \code{softplus(sim$psi)} in a
simulation study). Pass \code{NULL} (default) to skip the reference
comparison.}

\item{level}{Credible-interval level for coverage / width / interval-score
reporting (default \code{0.95}).}
}
\value{
An object of class \code{dgtf_ppc} (a list). Always present:
\itemize{
\item \code{chi} — averaged chi-square discrepancy of \code{y}
\item \code{Rt} — \verb{(ntime + 1) x 3} matrix of \code{R_t} posterior quantiles at
\verb{(alpha/2, 0.5, 1 - alpha/2)} with \code{alpha = 1 - level}
\item \code{width_Rt} — mean width of the \code{R_t} credible bands
\item \code{level} — the credible-interval level used
}

When \code{nrep > 0}:
\itemize{
\item \code{crps} — averaged continuous ranked probability score for \code{y}
\item \code{yhat} — \verb{(ntime + 1) x 3} quantile matrix of posterior-predictive \code{y}
\item \code{coverage_yhat}, \code{width_yhat}, \code{interval_score_yhat} — Winkler interval
score and its components for \code{y}
}

When a reference \code{Rt} is supplied (simulation studies):
\itemize{
\item \code{mae_Rt}, \code{rmse_Rt}, \code{coverage_Rt}, \code{interval_score_Rt}
}
}
\description{
Generates posterior predictive samples for an in-sample series under
the fitted model. Use this for posterior-predictive checks and
goodness-of-fit assessment.
}
\examples{
\dontrun{
fit <- dgtf(sim$y, mod, prior, method = "hva", control = vb_control())
pp  <- posterior_predict(fit, nrep = 100)
}
}
