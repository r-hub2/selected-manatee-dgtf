% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{rhat}
\alias{rhat}
\title{Split-Rhat (Gelman-Rubin) convergence diagnostic}
\usage{
rhat(x)
}
\arguments{
\item{x}{One of:
\itemize{
\item a numeric matrix with rows = iterations, cols = chains;
\item a list of equal-length numeric vectors (one per chain), giving
posterior draws of a single parameter;
\item a list of one or more \code{dgtf_fit} objects from independent MCMC
runs. HVA fits are rejected — they produce i.i.d. importance
samples, not chains, so split-Rhat is undefined.
}}
}
\value{
A scalar for matrix / list-of-vectors input. A named numeric
vector (one entry per scalar static parameter) for list-of-fits input.
}
\description{
Each input chain of length \code{n} is split sequentially into halves of
length \code{floor(n/2)} (the middle sample is dropped when \code{n} is odd), so
a single chain produces 2 sub-chains, two chains produce 4, and so on.
Values near 1 indicate the chains have mixed; > 1.01 is a common
warning threshold.
}
